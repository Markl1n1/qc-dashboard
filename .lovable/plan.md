
# План улучшения качества распознавания речи (VoiceQC)

## Текущее состояние

**Что уже реализовано:**
- Deepgram Nova-2/Nova-3 с автоматическим выбором модели по языку
- Keyterms только для Nova-3 (en, de, es, fr, ru, pl настроены в БД)
- Speaker diarization с `min_speakers=2`
- Базовый анализ качества аудио (`AudioQualityIndicator`)
- Мерджинг нескольких файлов в 8 kHz mono WAV

**Выявленные проблемы:**
1. 8 kHz sample rate — ниже рекомендуемого Deepgram (16 kHz)
2. Нет предобработки аудио (шумоподавление, нормализация)
3. Не используются Keywords API (boost слов с intensifier)
4. Нет Find & Replace для автоматической коррекции ошибок
5. Nearest-neighbor resampling теряет детали речи

---

## Блок 1: Повышение Sample Rate (Высокий приоритет)

### Проблема
`merge-audio-to-wav.ts` использует 8 kHz — это минимум для телефонии. Deepgram рекомендует 16 kHz для лучшего распознавания.

### Решение
1. Изменить `TARGET_SR = 16000` вместо `8000`
2. Заменить nearest-neighbor на линейную интерполяцию
3. Добавить anti-aliasing фильтр перед даунсемплингом

### Файлы
- `src/lib/merge-audio-to-wav.ts` — увеличить sample rate
- `src/lib/upload-and-transcribe.ts` — обновить комментарии

### Ожидаемый эффект
+10-15% точности распознавания, особенно на согласных и шипящих

---

## Блок 2: Предобработка аудио (Средний приоритет)

### Проблема
Аудио отправляется "как есть" — фоновый шум, разная громкость ухудшают результаты.

### Решение: Клиентская обработка через Web Audio API

```text
                        ┌──────────────────────────────────────────────────────┐
                        │              AudioPreprocessor Pipeline              │
                        ├───────────┬───────────┬───────────┬─────────────────┤
                        │ High-pass │ Low-pass  │ Dynamic   │  Loudness       │
                        │ Filter    │ Filter    │ Compressor│  Normalize      │
                        │ (80 Hz)   │ (8 kHz)   │           │  (-16 LUFS)     │
                        └───────────┴───────────┴───────────┴─────────────────┘
```

### Конкретные шаги
1. **High-pass фильтр (80 Hz)** — убирает низкочастотный гул
2. **Low-pass фильтр (8 kHz)** — убирает артефакты
3. **Динамический компрессор** — выравнивает громкость
4. **Нормализация** — уже есть `normalizePeak`, включить по умолчанию

### Файлы
- Новый: `src/utils/audioPreprocessor.ts`
- Изменить: `src/lib/merge-audio-to-wav.ts` — добавить pipeline

### Ожидаемый эффект
+5-10% точности на зашумлённых записях

---

## Блок 3: Keywords API с Intensifiers (Высокий приоритет)

### Проблема
Используется только `keyterm` (Nova-3). Keywords API работает для Nova-2 и даёт буст конкретным словам.

### Решение
1. Добавить параметр `keywords=WORD:INTENSIFIER` для Nova-2
2. Создать UI для управления Keywords в Settings
3. Хранить в `system_config` как `keywords_boost_[lang]`

### Формат
```
keywords=LLB Alpha:10&keywords=Revolut:5&keywords=инвестиция:8
```

### Файлы
- `supabase/functions/deepgram-transcribe/index.ts` — добавить keywords
- Новый: `src/components/KeywordsBoostSettings.tsx`
- `src/store/enhancedSettingsStore.ts` — добавить CRUD

### Ожидаемый эффект
Существенное улучшение распознавания терминологии компании

---

## Блок 4: Find & Replace для автокоррекции (Средний приоритет)

### Проблема
Deepgram иногда выдаёт "LLB Alfa" вместо "LLB Alpha". Нет автозамены.

### Решение
Использовать Deepgram Find & Replace API:
```
replace=LLB Alfa:LLB Alpha&replace=Ревелут:Revolut
```

### Реализация
1. Новая таблица `transcription_replacements` (pattern, replacement, language)
2. UI для управления заменами в Settings
3. Edge function применяет замены при запросе

### Файлы
- SQL миграция для `transcription_replacements`
- `supabase/functions/deepgram-transcribe/index.ts`
- Новый: `src/components/TranscriptionReplacements.tsx`

### Ожидаемый эффект
Автоматическое исправление повторяющихся ошибок

---

## Блок 5: Улучшение качества ресемплинга (Средний приоритет)

### Проблема
Nearest-neighbor ресемплинг теряет информацию. Особенно критично при понижении с 44.1/48 kHz.

### Решение
1. Использовать `OfflineAudioContext` для качественного ресемплинга
2. Web Audio API имеет встроенный anti-aliasing

### Код
```typescript
// Вместо nearest-neighbor
const offlineCtx = new OfflineAudioContext(1, newLength, TARGET_SR);
const source = offlineCtx.createBufferSource();
source.buffer = originalBuffer;
source.connect(offlineCtx.destination);
source.start();
const resampled = await offlineCtx.startRendering();
```

### Файлы
- `src/lib/merge-audio-to-wav.ts`

### Ожидаемый эффект
+3-5% точности за счёт сохранения деталей

---

## Блок 6: Опциональное серверное шумоподавление (Низкий приоритет)

### Проблема
Клиентская обработка ограничена. Для сложных случаев нужен FFmpeg.

### Решение
Новая Edge Function `audio-preprocess`:
```bash
ffmpeg -i input.wav -af "highpass=f=80,lowpass=f=8000,\
       acompressor=threshold=0.5:ratio=3,\
       loudnorm=I=-16" output.wav
```

### Реализация
- Использовать `@ffmpeg/ffmpeg` WASM или серверный FFmpeg
- Опциональный флаг `preprocessAudio: true` в UI
- Показывать индикатор "Enhanced processing"

### Файлы
- Новый: `supabase/functions/audio-preprocess/index.ts`
- UI toggle в Upload форме

### Ожидаемый эффект
Значительное улучшение на плохих записях (колл-центр, телефония)

---

## Блок 7: Индикатор качества с рекомендациями (Низкий приоритет)

### Проблема
`AudioQualityIndicator` показывает статус, но не предлагает действий.

### Решение
1. Анализировать аудио ДО отправки
2. Предлагать "Улучшить качество" если обнаружен шум
3. Показывать preview waveform с проблемными участками

### Файлы
- `src/components/AudioQualityIndicator.tsx` — расширить
- Новый: `src/utils/audioAnalyzer.ts`

---

## Приоритеты реализации

| # | Блок | Сложность | Влияние | Приоритет |
|---|------|-----------|---------|-----------|
| 1 | Sample Rate 16 kHz | Низкая | Высокое | 🔴 Критичный |
| 3 | Keywords API | Средняя | Высокое | 🔴 Критичный |
| 2 | Предобработка | Средняя | Среднее | 🟡 Важный |
| 4 | Find & Replace | Средняя | Среднее | 🟡 Важный |
| 5 | Качественный resample | Низкая | Среднее | 🟢 Желательный |
| 7 | Индикатор качества | Низкая | Низкое | 🟢 Желательный |
| 6 | Серверный FFmpeg | Высокая | Высокое | ⚪ Опционально |

---

## Быстрые wins (можно сделать сразу)

1. **`normalizePeak: true` по умолчанию** — одна строка в `merge-audio-to-wav.ts`
2. **16 kHz вместо 8 kHz** — изменить константу
3. **Добавить Keywords для Nova-2** — дополнить edge function

---

## Техническая сводка

### Изменяемые файлы
- `src/lib/merge-audio-to-wav.ts` — sample rate, resample, normalize
- `supabase/functions/deepgram-transcribe/index.ts` — keywords, replace
- `src/components/KeytermSettings.tsx` → расширить для Keywords

### Новые файлы
- `src/utils/audioPreprocessor.ts` — клиентская обработка
- `src/components/KeywordsBoostSettings.tsx` — UI для Keywords
- `src/components/TranscriptionReplacements.tsx` — UI для замен

### Миграции БД
- `transcription_replacements` (pattern, replacement, language, is_active)
- `system_config` записи для `keywords_boost_[lang]`
